# Stripe (use the synagogue's Stripe account; start with test keys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Optional: only used when the request host can't be determined
BASE_URL=http://localhost:3000

# Admin dashboard password (/admin)
ADMIN_PASSWORD=choose-a-long-password

# Optional: Google Sheet ticket auditor (see README + docs/google-sheet-auditor.gs)
SHEET_WEBHOOK_URL=
SHEET_WEBHOOK_SECRET=
